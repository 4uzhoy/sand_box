/*
import 'package:sand_box/config/platform.dart';
import 'package:workmanager/workmanager.dart';

class WorkmanagerCore {
  static bool isInited = false;

  static void initWM({Function callbackDispatcher}) {
    Workmanager.initialize(
        callbackDispatcher, // The top level function, aka callbackDispatcher
        isInDebugMode: Platform
            .isDebug // If enabled it will post a notification whenever the task is running. Handy for debugging tasks
        );
    isInited = true;
  }
}
*/
